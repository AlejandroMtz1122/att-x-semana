import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.attmx.attxsemana',
  appName: 'ATT x Semana',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
